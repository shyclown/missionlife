<IfModule mod_rewrite.c>
    RewriteEngine On
    Options FollowSymLinks

    RewriteBase /missionlife/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ /missionlife/#/$1 [L]
</IfModule>
